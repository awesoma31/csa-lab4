let array = list(5);

while i<n {
    print(list[i]);
    n = n + 1;
}



inter 0 { // read len
    n = read();
    iret;
}

inter 1 {
    let swapped = 1;

    while (swapped == 1) {
        swapped = 0;
        let i = 0;
        while (i < n - 1) {
            if (arr[i] > arr[i + 1]) {
                let temp = arr[i];
                arr[i] = arr[i + 1];
                arr[i + 1] = temp;
                swapped = 1;
            }
            i=i+1;
        }
        n=n-1;
    }
    iret;
}
